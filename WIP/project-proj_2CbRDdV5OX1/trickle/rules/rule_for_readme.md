When updating the project structure, adding new features, or changing existing core functionality:
- Always check if the `README.md` in `trickle/notes` needs to be updated.
- Keep the feature list, architecture description, and setup instructions current.